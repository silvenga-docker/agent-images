#!/command/execlineb -P
with-contenv
foreground { mkdir -p $XDG_RUNTIME_DIR/podman }
foreground { rm -f $XDG_RUNTIME_DIR/podman/podman.sock }
/usr/bin/podman system service --time=0 unix://$XDG_RUNTIME_DIR/podman/podman.sock
