#!/command/execlineb -P
with-contenv
/bin/bash -c "for i in $(seq 1 30); do [ -S /var/run/docker.sock ] && exit 0; sleep 1; done; echo 'dockerd-ready: timed out after 30s waiting for /var/run/docker.sock' >&2; exit 1"
