services:
  agent:
    build: .
    image: agent-image
    container_name: agent
    ports:
      - "4097:4096"
      - "3001:3000"
    volumes:
      - agent-home:/home/agent
      - docker-data:/var/lib/docker
    privileged: true
    environment:
      - OPENCHAMBER_ALLOW_UNAUTHENTICATED_LAN=true
    # runtime: sysbox-runc  # Enable when running with Sysbox; replace privileged: true

volumes:
  agent-home:
  docker-data:
